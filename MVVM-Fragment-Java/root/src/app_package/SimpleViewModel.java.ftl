package ${packageName}.ui.${moduleName};

import ${packageName}.base.BaseViewModel;
import ${packageName}.repository.${moduleName}.${repositoryClass};

import javax.inject.Inject;


public class ${viewModelClass} extends BaseViewModel<${repositoryClass}> {

	@Inject
    public ${viewModelClass}() {
    
    }
}